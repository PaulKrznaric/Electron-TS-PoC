{
  "name": "twillio-electron-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "npm run build && electron ./compiled/electron-app",
    "build": "./node_modules/.bin/tsc",
    "preinstall": "npm install && npm install --save-dev electron && npm install --save-dev typescript",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^21.2.0",
    "typescript": "^4.8.4"
  }
}